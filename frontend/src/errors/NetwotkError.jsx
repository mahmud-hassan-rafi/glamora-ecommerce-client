import React from "react";

const NetwotkError = () => {
  return (
    <h1
      style={{
        color: "crimson",
        textAlign: "center",
      }}
    >
      Connect to the internet
    </h1>
  );
};

export default NetwotkError;
